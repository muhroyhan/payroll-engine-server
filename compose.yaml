services:
  db:
    image: postgres:18.2-trixie
    restart: unless-stopped
    user: postgres
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=payroll-engine
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
volumes:
  db-data: